import type { WaterToday } from './types';

/** Realistic placeholder data matching the approved UI reference. */
export const WATER_TODAY: WaterToday = {
  cups: 5,
  goalCups: 8,
};

/** Quick-add amounts in cups mode. */
export const QUICK_CUPS = [1, 2, 2.5];

/** Common bottle sizes in ounces mode. */
export const BOTTLE_SIZES_OZ = [8, 12, 16.9, 20, 24, 32, 40, 64];
