# VITA — Audit Log

Single source of truth for audit findings and recommendations.

Per the Build Handbook, every audit entry includes: Issue, Reason, Recommendation, Decision, Status.

---

## Audit Entries

| Date | Slice | Issue | Reason | Recommendation | Decision | Status |
|------|-------|-------|--------|----------------|----------|--------|
| 2026-09-15 | Founder correction pass — Dashboard Fuel compact hierarchy | **The prior correction overshot the request.** The widget became a hero card and gave consumed and remaining equal visual weight — two 26pt figures side by side with the goal stranded on a quiet third line. Measured on a simulator at the default text size: **159.7pt tall** | Founder physical-iPhone review. The first correction had been asked to fix a truncated prose line and did, but it solved the truncation by growing the card and promoting the remainder to a co-equal statistic. Two large numbers leave the reader to work out which one the day is; and a Dashboard module that dwarfs the pair beneath it stops reading as one widget among several | One primary relationship — consumed out of goal — with the remainder demoted to a quiet secondary line. Hierarchy is consumed, goal, remaining. Content-driven height, no arbitrary replacement floor | **Fixed.** `180 / 1,500 cal` as one `Text` in three weights over `1,320 left`; `minHeight: 116` **deleted** rather than lowered (it was inert — the content measured nearer 160, so the floor never bound anything). `calorieSummary` traded `statFigure`/`statLabel`/`goalLabel` for a single bare `goalFigure`, so every calorie string still comes from one file. Measured **128.3pt**, a 19.7% reduction, on the same simulator at the same text size | ✅ Resolved |
| 2026-09-15 | Founder correction pass — Dashboard Fuel compact hierarchy | **128.3pt is above the founder's ~88–100pt estimate, and that estimate is not reachable under the same brief's other constraints** | Arithmetic, confirmed against the device measurement. §12 preserves the top row, whose height is set by the `Log` pill's shared `minHeight: 40`; module padding is `spacing.m` twice; §10 asks for a calorie line, a secondary line and a rail. 24 + 40 + 4 + 31 + 2 + 18 + 4 + 4 = 127pt is the floor, and the measurement agrees to within a point. The only lever left is the header row, which is 31% of the card and which §12 protects | Report the arithmetic rather than hit the number by shrinking the header against §12, or by re-hardcoding a `minHeight` below the content (which §11 forbids and which is the defect just removed) | **Reported.** Founder call on whether the 40pt header row may shrink | 🟡 Open — founder question |
| 2026-09-15 | Founder correction pass — Dashboard Fuel compact hierarchy | **An explicit `lineHeight` left 95pt of empty space above the calorie figure at accessibility-extra-large.** The card reached 359pt for one line of type and two short ones | Caught in this pass's own simulator verification, in code written the same hour, by measuring ink bands in a screenshot rather than by eye. The ratio-scaled `lineHeight` is the right tool for Fuel Home's headline, where a *fixed* 37pt line box had become a ceiling — but this line is three runs of different sizes, and the override produced phantom leading above and below instead | Remove it. Natural leading scales with the type by definition, and this line sits alone in its own gapped column, so there is no sibling for an overflowing line box to land on | **Fixed and re-measured:** 359pt → 232pt at the same text size, with proportionate leading between all three elements. The figure stays whole; no mid-number break | ✅ Resolved |
| 2026-09-15 | Founder correction pass — rail refinement | **Fuel Home and Home drew the calorie rail separately** — the same `ProgressBar` call, the same 3pt height, the same amber, written out twice in two files | Found while auditing the rail as §13 directed. Identical by hand rather than by construction, which is how two surfaces drift; and the founder's instruction was explicit that the refinement must not become Dashboard-only styling | One shared `AccentRail` in `components/ui/`, used by both. `ProgressBar` gained an optional `track` colour, unset at every pre-existing caller so `StatBar`, `DailyProgressCard` and Fuel's protein rail are byte-identical | **Fixed.** Track is now a low-opacity tint of the fill — the treatment VITA already uses for the flame's orb and the Peptides mark — instead of the neutral grey the founder called dead-looking; 3pt → 4pt so the capsule reads as one. **This changed Fuel Home, a locked screen** — deliberately, because §13 forbids the alternative. Verified on device in both themes | ✅ Resolved |
| 2026-09-15 | Founder correction pass — rail refinement | **Fuel Home's protein macro rail still carries the neutral grey track**, beside a calorie rail that no longer does | Observed in device verification. §13 named the calorie rail; the protein rail is a different object at a different scale (2pt, violet, under a macro value) and sweeping it in would extend an authorised rail refinement into a broader change to a locked screen | Leave it. `AccentRail` exists if the founder wants the macro rails to follow | **Reported, not changed.** Founder call | 🟡 Open — founder question |
| 2026-09-15 | Founder correction pass — Peptides first-use state | **The neutral-primary CTA was too dominant on an otherwise-empty screen** — the open question raised on 2026-09-13, now ruled on | Founder review confirmed the flag: `variant="neutral"` is the right weight for the commit at the end of a flow and far too loud as the only object on a near-black page. A white slab in place of a violet one | The outlined treatment Fuel Home's `Add food` has used since 5.6B, promoted to the shared `Button` rather than redrawn — §24 asked for an existing VITA treatment, not a new style | **Fixed.** `Button` gained `variant="outline"`: hairline border in `surfaces.border`, `surfaces.card` interior, label at full contrast, `color` spent on the glyph alone — violet here. The two feature-home primaries now render identically. The eight hand-drawn instances of this treatment elsewhere are left alone; migrating them belongs to 5.7 | ✅ Resolved |
| 2026-09-15 | Founder correction pass — Peptides first-use state | **The empty state had no content worth the space it occupied** — the second open question from 2026-09-13, now ruled on | The prior pass declined to fill the space because every candidate required fabricated routine data, and recorded it as a founder question. §25's answer is that the useful content is not product data at all: a first-use note about professional guidance | Zero-routine state only, restrained accent, and copy that keeps VITA a tracking tool | **Fixed.** `ⓘ BEFORE YOU BEGIN` in muted violet over *VITA is for tracking. Consult a qualified healthcare professional before starting or changing a routine.* — the founder's copy verbatim, above a hairline, not in a card, never red. Bound to `peptides.isEmpty`, so it cannot reach the populated screen. Tests assert it says none of *VITA recommends*, *what dose*, *supervised*, *prescribed*. The top gap dropped from 64pt to 24pt: the composition now reaches down the page on its own. **Space below the note is left deliberately** — §27 rules whitespace acceptable and density is not the objective | ✅ Resolved |
| 2026-09-15 | Founder correction pass — test integrity | **A style assertion had been passing vacuously.** `no longer floods the screen with a saturated violet slab` read `cta.props.style` on the node `control()` returns — which carries the handlers and the spoken name, not the fill | `PressableScale` applies its `style` to an inner `Animated.View`, the flex trap already recorded in the Migration Guide. `[cta.props.style].flat(4)` therefore returned an empty array, and `expect([]).not.toContain(palette.peptide)` passes on nothing | Read the node that actually paints, and assert the array is non-empty first so the test cannot go quiet again | **Fixed.** Rewritten to walk into the pressable for the painted style, with `expect(fills.filter(Boolean).length).toBeGreaterThan(0)` as a vacuity guard, and extended to assert the border and the violet glyph | ✅ Resolved |
| 2026-09-13 | Founder-directed post-closeout visual correction | **Peptides' zero-routine empty state was under-polished relative to the locked populated experience.** A title, one primitive line and a full-width saturated violet pill, above a large expanse of empty black | Found during founder physical-iPhone QA of the maintenance build. **Not a maintenance regression** — `git diff sprint-5-identity-interaction..maintenance/expo-sdk-57-patches -- src` is empty, so the state was identical on both branches. It predates the neutral-primary work: the local CTA carried a comment saying the shared `Button` could not be used because it took no `accessibilityLabel`, and **that stopped being true in 5.6D**, when `Button` gained both the label and `variant="neutral"`. The reason for the one-off had quietly expired | Reuse the mark the approved compact module already draws, refine the copy, and replace the pill with the shared neutral primary at its own width | **Fixed.** 72pt `medical` glyph in `palette.peptide` on a 10% violet ground; `Add a routine to start tracking your schedule and activity.`; `Button variant="neutral"`, `alignSelf` so it sizes to its label. Group dropped into the upper-middle rather than hugging the header | ✅ Resolved |
| 2026-09-13 | Founder-directed post-closeout visual correction | **Home's Fuel widget compressed three facts into prose** — `180 cal consumed · 1,320 left · 1 of 4 meals` inside a single `Text` capped at one line, which truncated with an ellipsis on a real device | Same attribution: pre-existing on both branches, exposed by device QA. The data was correct and the shared `calorieSummary` was already the single source; the fault was hierarchy. Consumed, remaining and the goal are three facts of descending importance, and a sentence flattens them. The 64pt module height was the constraint that forced the compression | Rebuild as statistics — figure over label, each its own node — and let the widget be taller. Keep the shared derivation | **Fixed.** Top row identity + `Log`; two statistics; quiet `1,500 goal`; Fuel-orange rail. `minHeight` 64 → 116. **Meal count removed** (§26 default). `calorieSummary` gained `statFigure`, `statLabel` and `goalLabel` so every calorie string still comes from one file | ✅ Resolved |
| 2026-09-13 | Founder-directed post-closeout visual correction | **A figure broke mid-number at accessibility-extra-large** — `1,320` rendered as `1,32` / `0` in the rebuilt widget | Caught in this pass's own device verification, in code written minutes earlier. The stat blocks carried a fixed `flexBasis`, so the row never wrapped: both statistics still "fitted" two across and each was squeezed to half a 390pt card. Scaling that basis by `fontScale` was a second miss for the same reason — the wrap has to follow the text actually rendered, not a number multiplied by a guess | `flexShrink: 0` with an automatic basis, so a statistic is never compressed below its content and `flexWrap` breaks the row when the two genuinely do not fit | **Fixed and re-verified on device** at accessibility-extra-large: the statistics stack one per line and the figure is intact | ✅ Resolved |
| 2026-09-13 | Founder-directed post-closeout visual correction | **The Peptides empty state still carries noticeable empty space below the content group**, after the identity mark, copy and CTA corrections | The founder's §8 offered an optional empty weekly scaffold to occupy it. Reusing `RoutineDayStrip` would require fabricating seven `StripDay` marks and an `onSelectDay` handler for a routine that does not exist — the fake schedule data §8 forbids — and `RoutineDayMark` has no value meaning *no routine*; the nearest, `not-scheduled`, asserts something about a routine that exists. Building a parallel scaffold is the duplication the standing contract forbids | §8's own escape clause: skip it, the identity mark alone is enough. The three problems the founder named — the purple slab, the missing identity, the old-VITA feel — are fixed; the remaining space is inherent to a one-action first-use screen | **Reported, not filled.** Founder call on whether it still reads as empty | ✅ Resolved 2026-09-15 — §25 ruled: fill it with the first-use guidance note, not with product data. The refusal to fabricate a weekly scaffold was upheld (§28) |
| 2026-09-13 | Founder-directed post-closeout visual correction | **`Button variant="neutral"` is a bright white block on the otherwise-empty Peptides screen** — arguably trading a saturated violet slab for a high-contrast white one | Observed in device verification. §10 named the neutral primary explicitly, and on Food Detail the same treatment sits under a full screen of content; here it is the only object. Fuel Home's equivalent feature-home primary (`Add food`) is *outlined* rather than filled, which would be quieter | Left as the founder specified. Worth a ruling: outlined would be more consistent with the other feature-home primary, filled-neutral more consistent with the app's primary-action philosophy | **Implemented as specified, flagged for a ruling** | ✅ Resolved 2026-09-15 — §24 ruled for outlined, matching Fuel Home's `Add food`. The flag was correct |
| 2026-09-13 | Expo SDK 57 patch maintenance | **The router logs `[Layout children]: No route named "(auth)" exists in nested children` on every launch.** `src/app/_layout.tsx` declares `<Stack.Screen name="(auth)" />`, but `src/app/(auth)/` holds only `sign-in.tsx` with no `_layout.tsx`, so the group flattens to the route `(auth)/sign-in` and the declared name matches no child | Observed while verifying the `expo-router` 57.0.19 → 57.0.21 bump, so it had to be established whether the bump caused it. **It did not:** the emitting `console.warn` is byte-identical in both versions' `useScreens` module, checked by unpacking both tarballs. Pre-existing since the app shell, and functionally harmless — `/sign-in` resolves and renders, verified on the updated build | Delete the unmatched `<Stack.Screen>` or give `(auth)` its own `_layout.tsx`, whichever the real auth work wants. It belongs with whichever slice implements sign-in, not with dependency maintenance | **Not changed.** §16 forbids product changes during maintenance; a dev-only warning with no functional effect does not justify touching the root layout on a maintenance branch | 🟡 Open — carried |
| 2026-09-13 | Slice 5.6E — Fuel end-to-end audit | **A logged food's name was cropped inside a meal on Fuel Home.** `TodaysMeals` carried `numberOfLines={1}` on the food name, so a 74-character provider name became an ellipsis once the food was in a meal — while the same food read in full in Search | 5.6C removed exactly that cap from `FoodListRow` after founder device QA saw `Clif Bar Cool Mint…`, and recorded the ruling that the name is the one string that must never be cropped. The ruling was applied to the file it was written on and not swept across the other implementations of the same pattern. At accessibility text sizes the row showed a few characters of the string a person is scanning for | Delete the prop. The row is `minHeight`, never fixed height, so it grows; the supporting serving line keeps its cap, because a secondary line may truncate and the subject may not | **Fixed.** A change to a locked screen, enumerated under §22 — one deleted prop, no effect at default text size with an ordinary name | ✅ Resolved |
| 2026-09-13 | Slice 5.6E — Fuel end-to-end audit | **`kcal` reached user-facing copy on both calorie-goal editors** — `/fuel/setup` and Settings → Nutrition Goals both labelled the same number `Daily calorie goal (kcal)` and spoke it as "in kcal" | The Design System has said since Sprint 2 that user-facing copy says **Calories**, or **cal** where a row is tight, never `kcal` — presentation only, internal fields unaffected. Two screens edited one value and both broke one written rule, invisible because every test asserted behaviour and none read a label | `(cal)`, matching the `(g)` and `(fl oz)` beside it. Guard it with a string-literal assertion across the whole source tree rather than a per-screen render | **Fixed** on both screens, with a repository-wide test | ✅ Resolved |
| 2026-09-13 | Slice 5.6E — Fuel end-to-end audit | **Two primary-action geometries exist in the product.** `/fuel/setup`'s Save is a full pill (`borderRadius: 999`); Food Detail, Manual Entry and Fuel Home's *Add food* use `radii.control` | It reads as a Fuel inconsistency until the surfaces are counted: the pill is what **locked** Water and Peptides CTAs, Peptides setup and Settings → Nutrition Goals all use. Changing Fuel Setup would make it diverge from four approved screens to match three | A shared button-vocabulary decision, not something a Fuel audit should settle unilaterally. Belongs with 5.7 or 5.8 | **Reported, not changed.** Founder call | 🟡 Open — carried |
| 2026-09-13 | Slice 5.6E — Fuel end-to-end audit | **Calories inside an expanded meal read `210`, not `210 cal`**, while every discovery row shows the unit | The meal's own subtotal directly above says `210 cal · 1 food`, so the column is unambiguous, and the accessible label speaks "calories" either way. Defensible density in a dense collapsible list; §21 warns against forcing identical geometry where context legitimately differs | Leave it, and put the question to the founder — it is a real difference and the call is theirs | **Flagged, not changed** | 🟡 Open — founder question |
| 2026-09-13 | Slice 5.6E — Fuel end-to-end audit | **Expo patch drift has widened from two packages to nine** — `expo`, `expo-blur`, `expo-camera`, `expo-constants`, `expo-font`, `expo-haptics`, `expo-linking`, `expo-router`, `expo-splash-screen`, each one patch behind the SDK 57 expectation | `package.json` and the lockfile are **byte-identical to `cf53a01`**; these are patch releases Expo published upstream in the four days since 5.6D. Doctor still reports 20/21 (one check failed). `expo-camera` is now on the list and the scanner depends on it | Its own maintenance slice, with a device pass afterwards. Not a Fuel concern, and §55 excludes it from 5.6E | **Not changed in 5.6E. RESOLVED 2026-09-13** on `maintenance/expo-sdk-57-patches` — all nine aligned via `expo install --fix`, Doctor 20/21 → 21/21, `package.json` + lockfile only. Awaiting founder device QA before merge | ✅ Resolved |
| 2026-09-13 | Slice 5.6E — Fuel end-to-end audit | **`/fuel/log` remains old-VITA — a `Card` and a filled-orange `+ Log Food`** — and `LoggedEntryRow` exists only to serve it | Re-audited: it has **zero references** in `src/`, the same state recorded at the 5.6B audit. Nothing in normal navigation reaches it; it survives for deep-link back-compatibility | Leave it rather than redesigning or deleting it for tidiness, and assert the *unreachability* instead of the appearance — if a later slice surfaces the route, the test fails and the screen must be brought into the locked language first | **Left in place, now test-guarded** | 🟡 Open — intentional |
| 2026-09-01 | Slice 3.10A — final audit resolutions | **A test had been silently passing without testing anything.** `reads an mcg-authored vial at a legible scale` opened with `if (!…findAll('Vial unit').length) return;` — a guard that early-returned on the setup form, and on the calculator too once the vial toggle was removed | It was written when only one of the two surfaces had the control, and the guard was never revisited. A test that returns before its first assertion is worse than no test: it is counted, it is green, and it is guarding nothing | Reach the same behaviour through a path that still exists. A 0.5 mg vial in 2 mL is 250 mcg/mL, so one whole milligram would be 400 units — off the end of the barrel — and the reference drops to a landmark that can actually be drawn | Rewritten against the milligram path, asserting `0.05 mg = 20 units` and that no row reads `400 units` | ✅ Resolved |
| 2026-09-01 | Slice 3.10A — final audit resolutions | Three existing tests asserted a scheduled-today routine appears under **ACTIVE** | Correct before the Today/Active deduplication and wrong after it. They were the suite noticing a deliberate behaviour change, which is what they are for | Update them to the new intent rather than weakening them: assert the routine is in Today, appears exactly once on the screen, and that the model still says `active` | Updated, and the "sorts every routine into exactly one section" test was strengthened to four routines across four sections with per-name occurrence counts | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **The unit-conversion reference could render two rows that read identically.** `1 mcg = 50 units` sat directly above `1 mcg = 100 units` — the same amount with two different answers | Micrograms display as whole numbers (`DISPLAY_DECIMALS.mcg = 0`), and `ROW_MULTIPLIERS` starts at `0.5`, so a true 0.5 mcg row rounds to "1 mcg". A sweep over realistic vial/volume pairs found 38 reachable cases, all through the standalone calculator's mcg vial toggle. The founder named this exact defect class in §15 | Guard in the model rather than in the view: never emit a row whose *rendered* amount repeats an earlier one. Display integrity is a property of the reference, not of whoever draws it | Fixed. The milligram path — every setup form, and the calculator's default — is byte-identical, pinned by a test asserting the founder's approved `0.5/1/2/3/4/5 mg` ladder still renders in full | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **A blue waterline drew across the Water panel for every user who had not set a goal** — the "empty vessel" the component's own documentation says it must never show | `level` correctly animates to `0`, but the surface line is a fixed 2pt child anchored to the *top* of that zero-height box, so it painted at the card's bottom edge. Invisible to every test and to code review; found by sampling pixels in a device screenshot of the first-run state | Do not render the fill layer at all when there is no goal. A level of zero states how much you have drunk; "you have not chosen a goal" is not that statement | Fixed, and re-verified on device: no blue pixels in the panel without a goal, waterline still correct at 44% with one | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **A test was holding a defect in place.** Slice 3.9A removed the Preferred Unit control and left its explanatory sentence on screen — *"How amounts are shown for this peptide. A display preference, not a recommended amount."* — floating between the conversion table and the Routine header, describing a control nobody could see | `SetupForm.test.tsx` asserted the copy was **present**. It was written when the control existed and was never revisited, so the orphan stayed green for two slices | Delete the sentence and invert the assertion: what the form must not do is explain a control it does not have | Fixed. Test now asserts the absence | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **Storage formats were leaking onto the routine screen**: the reminder read `09:00` and the start date read `2026-08-24` | Both were rendered as the strings they are persisted as. Everywhere else in the app speaks 12-hour time (`9:15 AM`) and written dates | Two shared formatters in `lib/daily`, hand-written for the same reason the existing ones are — Hermes' `Intl` support is not uniform enough to risk a silent fallback | Fixed: `formatTimeOfDay` and `formatLogDateWithYear`. The year matters — a routine's start date is often months old, and `formatLogDateLong` omits it | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **Three surfaces disagreed about how to name the same things.** "Edit Routine" opened a screen titled "Setup"; the standalone calculator asked for `Vial Amount (mg)` and `Bacteriostatic Water / Reconstitution (mL)` while Routine Setup asked for `Vial Amount (MG)` and `Reconstitution Volume (ML)`; and the setup form itself carried `Amount (mg)` four lines under `Vial Amount (MG)` | The 3.9A copy fix landed on the setup form and never reached the calculator. The founder flagged both the slash-heavy label (§37) and mixed unit casing (§38) | State the convention and apply it: a **configuration field label** caps its unit — `(MG)`, `(ML)` — and every **displayed value** stays lowercase — `2 mg`, `20 units`, `1.2 mL`. Retitle the screen to what the row that opens it promises | Fixed on all three, and the convention is pinned by tests and documented in `09-Technical-Documentation.md` | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **Water had no route-level test.** Its units, totals, goals, entries, week and provider are thoroughly covered — and no test had ever rendered a Water screen | Exactly the gap that let PT-141 ship broken: `searchCatalog` was correct with 92 passing tests while the screen showed users nothing they recognised. The founder called this out directly in §46 | Add screen-level regressions that drive the real routes: add a drink, set a goal, repeated adds, disabled save, load failure, and the midnight boundary | Added `src/features/water/__tests__/WaterRoutes.test.tsx` — 13 tests, including the one that caught the waterline bug | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | Smaller findings, all fixed: `PeptideRowPanel.tsx` and `VialSummary.tsx` were fully dead (zero references, orphaned by the 3.9 redesign); four files carried unused `StyleSheet` keys; the setup screen rendered the catalog category raw while every other surface title-cased it; the Schedule segmented control was the only one on the form with no `groupLabel`, so it announced four unattached buttons | Ordinary drift from a fast-moving redesign. `--noUnusedLocals` does not see dead style keys or dead components | Delete what is dead, format what is displayed, name what is tappable | All fixed. A dead-style sweep is now part of the validation run | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **`doseConsistencyNotes` is written, documented, tested — and wired to nothing.** It detects an amount larger than the whole vial, which is the signature of a typo in one of the two fields the syringe number is derived from | Orphaned by slice 3.6D, which replaced the dose input with the automatic reference. The check survived; the surface that called it did not. A mis-typed vial produces a coherent-looking table that is wrong by a factor of ten or a thousand, with nothing on screen to say so | **Do not wire it during a closeout.** Adding a new visible behaviour to a form the founder has just approved is scope creep, however useful. Recommended as a small, well-scoped slice | **Founder ruling (3.10A): do not expose it, and prefer deleting dead code over preserving speculative helpers. Removed.** Deleted alongside its three orphaned siblings in the same module — `calculateConcentration`, `calculateAmountFromUnits` and `calculateSyringeUnitsForMass`, all likewise zero-consumer since 3.6D replaced the dose input with the automatic reference. Keeping one and deleting the others would have been arbitrary. 80 lines of source and 16 tests removed; no UI behaviour changed, because none of it was reachable from a screen. The arithmetic is not lost — it is four lines against `calculateSyringeUnits` whenever a surface actually needs it | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **The standalone calculator still offers a mg/mcg vial toggle**, which Routine Setup deliberately lost in 3.9A. A vial entered as mcg instead of mg is wrong by a thousand, and every syringe number below it is wrong in the same direction | The existing code documents a considered counter-argument: the calculator persists nothing, so "a mistaken unit is visible and disposable". The audit's disagreement is that it is *not* visible — the table looks coherent either way, and the user acts on the number, not on the saved state. It was also the only route to the duplicate-row defect above, which is now closed independently | Founder call, not an engineering one. A documented deliberate decision should not be reversed inside a closeout audit | **Founder ruling (3.10A): Vial Amount is MG-only everywhere.** The toggle is gone from the standalone calculator, which now reads `Vial Amount (MG)` exactly as Routine Setup does. **The Custom Amount keeps mg and mcg** — that is the amount being converted, not the vial size. Dead state, callbacks, imports and styles removed with it, and a test that had been silently early-returning past the deleted control was rewritten to reach the same low-concentration behaviour through a genuinely small milligram vial | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **Routine Setup presents seven section headers** — VIAL, UNIT CONVERSION, ROUTINE, SCHEDULE, REMINDER, START DATE, NOTES — where the founder's §13 describes three: VIAL, ROUTINE (amount, schedule, reminder, start date), NOTES | Each header arrived with the slice that added its field. Nothing is wrong individually; together the form reads longer and more administrative than it is | Fold Schedule, Reminder and Start Date under ROUTINE, keeping each control's accessible group name so nothing loses its label | **Founder ruling (3.10A): simplify to VIAL / ROUTINE / NOTES.** Schedule, Reminder and Start date are now field-weight labels inside ROUTINE, and Unit conversion is a field-weight label inside VIAL rather than a section competing with it. Seven headings became three. **No functionality was removed** — every field is still present, pinned by a test that names all seven controls. Verified on device in Light and Dark | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **A single active routine appears twice on the Peptides home screen** — once under TODAY and again under ACTIVE. With one daily peptide the screen is literally the same name twice | By design: TODAY answers *what do I do now*, ACTIVE is the roster. The redundancy is invisible with five routines and obvious with one. The founder asked directly in §7 whether Active and Inactive are too repetitive | Consider omitting from ACTIVE any routine already shown in TODAY, or retitling ACTIVE to *All routines* so the repetition reads as intentional | **Founder ruling (3.10A): a routine shown in Today does not also appear under Active.** Implemented as **presentation filtering only** — membership is decided by setup id against what Today already renders, and `routineState` is neither read nor written, which a test asserts directly. As Needed and unscheduled routines are never in Today so they always appear under Active; Needs Setup and Inactive are untouched. The Active empty state now counts Today, or a user with one daily routine would have been told "Nothing active right now" directly beneath it | ✅ Resolved |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | **Peptide reference content has never had expert review.** 96 entries carry overviews, mechanisms, claims, studied-for and development status | Automated tests enforce *structure and internal consistency* — unique ids and aliases, no shared prose between similar compounds, no dosing or protocol language, every time-sensitive stage dated and sourced. None of that is a check on whether a sentence is medically accurate, and it must not be mistaken for one | **Release gate.** Before any public release, this content requires real content, medical and legal review appropriate to a consumer health product | Documented as a release gate. Not a Sprint 3 blocker | 🔴 Release gate |
| 2026-08-31 | Slice 3.10 — Sprint 3 closeout audit | Start Date is a free-text `YYYY-MM-DD` field, with a placeholder and an error message in the same form | It is the last raw storage format still visible on a configuration surface. A `Today` chip covers the common case | A real date picker, when a slice is scoped for it — building one during closeout is exactly the new subsystem §48 forbids | Not changed | 🟡 Open — future slice |
| 2026-08-21 | Sprint 2 closeout audit | **Favorites rendered its empty state before storage hydrated**, telling a user with saved favorites that they had none for the first frames after launch | `useNutrition()` exposes `status`, but the Favorites screen only checked `rows.length`. Recents guards this with its own `isLoading`, the Food Log with `pending`, and Fuel's summary holds an em dash rather than a false zero — Favorites was the one surface that did not | Show a spinner while `status === 'loading'`, matching the existing pattern rather than inventing a new one | Fixed in this pass — one screen, no new state, no new component | ✅ Resolved |
| 2026-08-21 | Sprint 2 closeout audit | **Sprint 2 has no committed test suite.** GTIN normalization, nutrition arithmetic, dedupe, ranking, and food classification are all pure and highly testable, but there is no `jest`/`vitest`, no test files, and no test script | Every slice verified its logic with throwaway scripts. 67 assertions were executed again during this audit against the real compiled modules — all pass — but none of it is repeatable by anyone else or in CI, so the next regression is found by a founder on a device rather than by a machine | Add a test runner and lift the ad-hoc assertions into committed tests. **Recommended as the first task of Sprint 3**, before new feature work builds on this domain | Not fixed here — adding a dependency and a harness is its own slice, not closeout scope | 🟡 Open — recommended for Sprint 3 |
| 2026-08-21 | Sprint 2 closeout audit | A synthetic ranking case suggested a generic USDA *Hamburger* could outrank *Big Mac — McDonald's* | Raised as a possible defect, then tested against the live providers: `big mac` actually returns *Big Mac (McDonalds)* first, `banana` returns *Bananas, raw* first, `chipotle chicken bowl` returns *Chipotle Chicken Bowl* first. The synthetic scenario — a Foundation-quality "Hamburger" returned for `big mac` — does not occur | **Withdraw the finding and change nothing**, per the standing instruction not to tune ranking without a real deterministic defect | No change made. Recorded so the same synthetic case is not "rediscovered" later | ✅ Resolved — not a defect |
| 2026-08-21 | Sprint 2 closeout audit | Tracker statuses were stale: slices 2.1–2.9 still read "Built, pending founder review" after the founders declared them approved, and 2.6 still said USDA was "blocked on founder key" | Status was written at build time and never revisited when approval arrived; the USDA key has since been configured and both providers now return results in a live run | Correct the tracker to match reality | Slices 2.1–2.9 marked ✅ Approved; 2.6 note corrected; Sprint 2 header moved to feature-complete/in-closeout | ✅ Resolved |
| 2026-08-21 | Sprint 2 closeout audit | `expo@54.0.36` and `expo-constants@18.0.13` sit one patch behind the SDK 54 expectation (`~54.0.37`, `~18.0.14`) | `expo install --check` reports it on every run. It is patch drift *within* SDK 54, not an SDK upgrade | Report rather than apply — a dependency change during closeout carries regression risk with no demonstrated need. Founder call | Not changed | 🟡 Open — founder decision |
| 2026-07-09 | Project Review (pre-Sprint 1) | `docs/04-Master-Roadmap.md` reflected the old 9-sprint structure (Foundation/Dashboard/Fuel/My Journey/Water/Peptides/Atlas-placeholder/Settings/Polish), contradicting the new 8-sprint roadmap adopted in Vita HQ | The HQ roadmap was overhauled 2026-07-09 without a corresponding repo doc sync | Rewrite `04-Master-Roadmap.md` to match the HQ roadmap | Synced to Foundation → Dashboard → Journey → Fuel → Atlas → Health → Premium → Beta | ✅ Resolved |
| 2026-07-09 | Project Review (pre-Sprint 1) | `docs/06-Slice-Tracker.md` contradicted itself: its table showed slices 0.2–0.12 as 🟡 Founder review while its "Completed Slices" section claimed 0.2–0.10 were approved, and the Changelog described 0.11–0.12 as already shipped | Tracker wasn't updated when the founders declared Sprint 0 complete in the new roadmap | Mark all Sprint 0 slices consistently, matching the founders' Sprint 0 completion decision | All Sprint 0 slices (0.1–0.12) marked ✅ Approved; Sprint 1 slices added as ⬜ Planned | ✅ Resolved |
| 2026-07-09 | Project Review (pre-Sprint 1) | No Sprint Retrospective existed for the 2026-07-09 roadmap-overhaul session, despite retrospectives being mandatory per the sprint workflow | The retrospective step was skipped during that session | Log the missing retrospective in Vita HQ | Added to `00 HQ/Sprint Retrospectives.md` | ✅ Resolved |
| 2026-07-09 | Project Review (pre-Sprint 1) | Open Questions #1/#2 (theme direction: dark-only vs. current light theme) and #3 (Settings dock placement) blocked two Sprint 1 slices (Dashboard Polish, Floating Navigation) | Undecided design direction risked rework once Sprint 1 UI work began | Get founder decisions before Sprint 1 starts | Founders decided: VITA supports Light + Dark Mode via semantic design tokens, not hardcoded colors; Settings stays permanently in the top-right corner, never the dock | ✅ Resolved |
| 2026-07-09 | Project Review (pre-Sprint 1) | This Audit Log had zero recorded entries despite ~12 shipped Sprint 0 slices and a mandatory audit step in the ten-step slice lifecycle | Sprint 0 slices went to founder review without formal audit entries | Begin logging audits starting now | This entry is the first recorded audit | ✅ Resolved — continue logging per slice going forward |
| 2026-08-02 | Sprint 1 — Dashboard, final cleanup pass | Several rounds of Home redesign iteration (theme system + "Mountain World" abandonment + clean redesign + density pass) left dead code behind: an unused `radii.glassMedium` token, an unused `surfacesFor()` helper duplicating logic `ThemeProvider` reimplements inline, `ListRow`'s unused `surface` prop, `Screen`'s unused `background` prop (both left over from the abandoned photo-hero background), dead `DashboardData.headline`/`subline` fields that `HomeHeader` never actually reads, and an unused `expo-linear-gradient` dependency (also installed for the abandoned hero) | Fast iteration under founder direction is the right call, but it accumulates orphaned code that should be swept before the next sprint builds on top of it | Full sweep: `npx tsc --noEmit --noUnusedLocals --noUnusedParameters` (diagnostic-only, zero findings — ruled out unused imports/locals as a category), grep-verified zero consumers for every proposed removal before touching it, categorized findings (safe / needs review / no-action) and got explicit founder approval before deleting anything | Removed: `radii.glassMedium`, `surfacesFor()`, `ListRow.surface`, `Screen.background`, `DashboardData.headline`/`subline`, `expo-linear-gradient` (via `npm uninstall`, not a manual `package-lock.json` edit). Kept: `PressableCard.tsx` (reserved for Sprint 2 Fuel per founder decision), `QuickStatsRow`/`QuickStat` naming (cosmetic only, not worth the churn), `mealIcons.ts` and its apparent overlap with `MealRow`'s own icon map (genuinely different underlying types — Fuel's singular `'Snack'` vs. Dashboard's plural `'Snacks'` — merging would violate the no-cross-feature-import rule) | ✅ Resolved |
| 2026-08-21 | Sprint 2 slice 2.10 — FatSecret research checkpoint (Phase 1) | **FatSecret's Developer Terms forbid retaining nutrition Content beyond 24 hours, which conflicts with VITA's food-log snapshot architecture** | Terms: *"You may not continue to use and must immediately remove or replace (by re-requesting) any Content in your possession or under your control not explicitly identified as being storable indefinitely within 24 hours after the time at which you obtained the Content."* Only identifiers are storable indefinitely (`food_id`, `serving_id`, `food_category_id`, `food_entry_id`, `recipe_id`, `saved_meal_id`, `auth_token`, `auth_secret`, `exercise_id`, `recipe_types`). Nutrition values, food names, brand names, serving details and image URLs are **not** on that list. VITA deliberately stores all of those permanently in `FoodEntry` so history survives provider revisions, cache expiry and offline use — and `readCachedFood` holds definitions for 30 days | Do not add `fatsecret` to `PERSISTABLE_SOURCES`. Present the founders with the architecture options rather than silently compromising the snapshot model, which is load-bearing product architecture | **Pending founder decision — integration not started** | 🟡 Open — blocking |
| 2026-08-21 | Sprint 2 slice 2.10 — FatSecret research checkpoint (Phase 1) | **The approved Edge Function architecture cannot satisfy FatSecret's OAuth 2.0 IP allowlist** | FatSecret docs: *"tokens can only be requested from a finite number of IP addresses that you will be able to specify when managing your keys"* (up to 15 for Basic). Supabase's own troubleshooting documentation states Edge Functions **cannot** provide static egress IPs — the Deno Deploy runtime allocates a different address per invocation. The documented workaround is a paid static-IP proxy, which reintroduces cost and breaks the $0 rule | Two candidate paths, neither yet confirmed: (a) OAuth 1.0 two-legged signed requests, which community reports suggest are not bound by the OAuth 2.0 IP allowlist — needs written confirmation from FatSecret; (b) host the gateway somewhere with a static egress IP instead of Supabase. Do not proceed on assumption | **Pending founder decision — integration not started** | 🟡 Open — blocking |
| 2026-08-21 | Sprint 2 slice 2.10 — FatSecret research checkpoint (Phase 1) | Attribution obligations extend beyond the app UI to the App Store / Google Play listing | Terms require a Web Badge or Attribution Snippet *"in your Application, on your Website and as part of your AppStore / Google Play listing"*, and attribution must appear somewhere reachable without logging in. Basic and Premier Free both require it; only paid Premier waives it | Treat store-listing attribution as a founder action, not an engineering one, and confirm it is acceptable before integrating | **Pending founder decision** | 🟡 Open |
